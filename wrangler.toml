name = "newgeneration-english"
compatibility_date = "2025-01-01"
pages_build_output_dir = "."

# AI binding — Cloudflare Workers AI (бесплатно в Pages free tier).
# Доступ через env.AI.run() внутри Functions.
[ai]
binding = "AI"
